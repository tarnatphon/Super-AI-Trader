# Core system runs on the Python standard library only.
# These are optional extras that unlock more features:

yfinance>=0.2.40        # real market data:  --real
openai>=1.30.0          # optional cloud LLM agents (default AI is fully LOCAL via Ollama)
ccxt>=4.4.0             # live/paper trading on 100+ exchanges: paper/talk commands
pytest>=8.0.0           # running tests

# Local AI (no cloud): install Ollama from https://ollama.com and run `ollama pull llama3.1`
# then `talk`/`paper` will use your local model automatically.
